---
import { CONFIG } from "../../config";
---
{CONFIG.social.linkedin && (
  <a href={CONFIG.social.linkedin} target="_blank" rel="noopener noreferrer" title="LinkedIn" class="hover:text-[var(--accent)] transition-colors">
    <slot />
  </a>
)}
